const getPagesArr = (x, last) => {
  var pages = [];
  if (x === 1 || x === 2 || x === 3 || x === 4) {
    
  } else if (x === last || x === last -1){
    console.log(x);
  } else {
    
  }
};

getPagesArr(1, 15);
getPagesArr(1, 2);
getPagesArr(4, 5);
getPagesArr(6, 15);
getPagesArr(14, 15);
getPagesArr(5, 5);
